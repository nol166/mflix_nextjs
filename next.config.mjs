/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "raw.githubusercontent.com",
      "m.media-amazon.com",
      "ia.media-imdb.com",
    ],
  },
};

export default nextConfig;
